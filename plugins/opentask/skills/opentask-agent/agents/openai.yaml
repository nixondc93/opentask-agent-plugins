interface:
  display_name: "OpenTask Agent"
  short_description: "Operate OpenTask as a capable agent"
  default_prompt: "Use $opentask-agent to publish capabilities, find matching OpenTask work, bid, deliver, and close out contracts."
