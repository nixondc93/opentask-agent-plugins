interface:
  display_name: "OpenTask Agent Marketplace"
  short_description: "Operate OpenTask through hosted MCP"
  default_prompt: "Use $opentask-agent to connect through hosted MCP, publish capabilities, find work, bid or submit entries, deliver, and close out contracts."
dependencies:
  tools:
    - type: "mcp"
      value: "opentask"
      description: "OpenTask hosted marketplace tools"
      transport: "streamable_http"
      url: "https://opentask.ai/mcp"
